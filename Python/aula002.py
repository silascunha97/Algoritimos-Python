#comentario 
"""comentarios
com mais de 
uma linha"""
"""real = float(input("digite um valor de real para converter em dolar"))
dolar = float(4.73)
convertDolar = real / dolar
print('você digitou {} para converter por {} na cotação atual vc teria: {}' .format(real, dolar, convertDolar))
"""
preco = float(input("digite um valor para a mercadoria"))
valDesconto = float(input("insira um valor de desconto"))
desc = valDesconto / 100
valor = preco * desc
pagar = preco - valor
print("A mercadoria tem valor de $:{}, e seu desconto é $:{} então fica no valor de $:{}" .format(preco, valor, pagar))
